# Application Settings
NODE_ENV=development  # Use 'development' for SQLite, 'production' for PostgreSQL
PORT=3000            # Port for the Express server to listen on

# Production Database Configuration
# These are only needed when NODE_ENV=production
DB_HOST=localhost    # PostgreSQL host address
DB_PORT=5432        # PostgreSQL port number
DB_USER=postgres    # PostgreSQL username
DB_PASSWORD=postgres # PostgreSQL password

# Note: In development, SQLite database will be created at ./db/mrgll-me.sqlite3
# Note: In production, database name is fixed as 'mrgll-me'
